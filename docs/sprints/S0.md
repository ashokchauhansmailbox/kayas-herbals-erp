# Sprint 0 — Foundation & Environment

## Status: In Progress
Sprint 0 is scaffolding only. No product features shipped.

## Deliverables checklist
- [x] Monorepo skeleton committed (`apps/`, `packages/`, `backend/`, `docs/`, `infra/`)
- [x] Brand-token package (`packages/ui/theme.ts`)
- [x] `.env.example` documenting every required key
- [x] Decisions doc frozen (`docs/DECISIONS.md`)
- [ ] pnpm-workspace.yaml + turbo.json (added at Sprint 1 start when apps get their first React code)
- [ ] Supabase project provisioned (dev + staging) — **needs your action, see below**
- [ ] GitHub Actions: lint + typecheck + build (added when CI account is provided)
- [ ] Alembic migration harness in `backend/` (added at Sprint 1 start)

## Notes on current Emergent environment
The existing `/app/backend` (MongoDB) and `/app/frontend` (React) services remain live so you can keep demoing the v0 preview. From Sprint 1, we will:
1. Replace `/app/backend` with the FastAPI + SQLAlchemy + Alembic stack targeting Postgres.
2. Move `/app/frontend` into `apps/admin` and add sibling `apps/web-store` + `apps/seller-portal`.
3. Point services at Supabase (dev project) using `.env` values below.

## Actions required from you before Sprint 1 can commit code
1. **Create a Supabase dev project** and share:
   - `SUPABASE_URL`
   - `SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `SUPABASE_JWT_SECRET`
   - `DATABASE_URL` (Postgres connection string from Supabase → Project Settings → Database)
2. **Razorpay test-mode credentials** (Key ID + Key Secret + Webhook Secret).
3. **Resend API key** for transactional emails.
4. **WhatsApp Cloud API** phone-number-id + access token.
5. Provide the Kaya's Herbals **logo file** (SVG preferred) so `packages/ui` can bundle it.

Once these are in `/app/.env` (or shared through the Emergent secrets UI), Sprint 1 will kick off with:
- Postgres schema v1 (users, roles, permissions, role_permissions, user_roles, audit_logs, activity_logs)
- Supabase Auth ↔ our RBAC bridge
- OpenAPI-3 auto-docs
- Seed script: 1 Super Admin + capability catalogue
- Demo video at S1 close

## Definition of Done (Sprint 0)
- Monorepo skeleton on disk ✅
- Brand tokens usable from any app ✅
- Decisions frozen & readable ✅
- Environment matrix documented ✅
- Every downstream sprint dependency has a clear next step ✅
