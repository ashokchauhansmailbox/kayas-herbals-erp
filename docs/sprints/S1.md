# Sprint 1 — Identity Core (In Progress, Credential-Independent Mode)

## Status
Scaffolding backend without live DB connectivity. Migrations authored, not executed. `docker-compose.yml` provided for local Postgres+Redis+backend so you (or CI) can run everything offline from cloud.

## What's committed
- `docker-compose.yml` — Postgres 16 + Redis 7 + backend service; single command boots the whole stack.
- `backend/Dockerfile`, `requirements.txt`, `.env.example` (all env vars documented with source).
- `backend/app/core/` — Settings (pydantic-settings, no defaults for secrets), structlog logging, unified error envelope.
- `backend/app/main.py` — FastAPI app, CORS, `/api/v1` mount, OpenAPI at `/api/v1/docs`, `/health`.
- `backend/app/api/v1/__init__.py` — module router wiring.
- Folder skeleton: `db/`, `models/`, `schemas/`, `repositories/`, `services/`, `middleware/`, `seeds/`, `tests/`, `migrations/versions/`.

## Still to be committed this sprint
1. `app/db/base.py`, `session.py`, `mixins.py` (SQLAlchemy declarative + async session + Timestamp/Actor/SoftDelete/Version mixins).
2. Models: identity, master_data (11 tables per doc 08), catalog stubs, `documents` polymorphic table, batch-recall columns on `batches`, future-module seams.
3. Alembic migrations `001` → `012` with reversible `downgrade()` + per-file docs under `/docs/database/`.
4. Repositories + services (Clean Arch): `UserRepo`, `RoleRepo`, `AuditRepo`; `AuthService`, `RbacService`, `AuditService (with AuditContext)`, `ActivityService`.
5. Middleware: Supabase JWT verifier (PyJWT, HS256, verify audience + issuer) → resolves local `user_id`; RBAC dependency `require("permission.code")`.
6. Pydantic schemas + routes for auth, users, roles, permissions, master-data, audit endpoints.
7. Seed script `app/seeds/run.py` — permissions catalogue (~120), 9 system roles, role↔perm mapping, units, GST rates, HSN, categories, brand, warehouse, payment terms, tax rules, courier partners. **No demo business data.**
8. `/docs/database/` — one MD per migration.
9. `/docs/api/openapi.json` — snapshot of auto-generated spec.
10. `/docs/ops/` — Backup, DR, Migration, Rollback, Restore, Version-Upgrade playbooks.
11. Pytest smoke tests (no DB): schema validation, JWT verifier unit tests, RBAC evaluator unit tests.

## How to run locally (once complete)
```
cp backend/.env.example backend/.env      # DATABASE_URL prefilled for docker-compose
docker compose up --build
# Backend @ http://localhost:8001/api/v1/docs
```

## Definition of Done (checkpoint for Sprint 1)
- [ ] All files above committed
- [ ] `docker compose up` boots stack, migrations apply cleanly, seeds run
- [ ] `alembic upgrade head → downgrade base → upgrade head` passes
- [ ] Pytest green
- [ ] OpenAPI snapshot updated in `/docs/api/`
- [ ] Sprint summary document authored

## Context-window note
This sprint required extensive file authoring. I've committed the foundation (compose, Dockerfile, settings, logging, errors, main app, router wiring, env template) in this session. **The remaining files (models, migrations, services, seeds, per-migration docs, ops runbooks, tests) will be committed in the next continuation**, since my available context window is now near its limit and I want each file to be production-quality rather than truncated.

Please reply **"continue Sprint 1"** and I will resume from item 1 above in a fresh context, delivering the complete Sprint 1 bundle plus the 8-item sprint summary for your review.
